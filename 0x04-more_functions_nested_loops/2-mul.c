#include "holberton.h"

/**
 *mul - determine if a given integer is a digit
 *@a:parameter to be evaluated
 *@b:parameter to be evaluated
 *Return:product of multiplication of two integers
 */

int mul(int a, int b)
{
	int l;

	l = a * b;
	return (l);
}
